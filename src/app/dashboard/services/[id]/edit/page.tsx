import { ServiceEditView } from 'src/sections/services/view';

export const metadata = {
  title: 'Dashboard: Service Edit',
};

export default function ServiceEditPage() {
  return <ServiceEditView />;
}